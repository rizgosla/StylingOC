The default link treatment across the system — prefer it over Button for anything that is not the section's single CTA.

```jsx
<TextLink href="/projects">See the full index</TextLink>
<TextLink caps tone="inverse" href="/services">Services</TextLink>
```

`caps` gives nav/caption styling. `tone="inverse"` for placement over dark imagery.
