Before / after — drag, or arrow keys. The core proof component for real-estate-agent pages.

```jsx
<BeforeAfter before="/assets/images/…" after="/assets/images/…" caption="Newport Coast · Pre-listing refresh" initial={46} />
```

Keep the two frames the same camera position. Labels stay in wide small caps, never in chips or badges.
