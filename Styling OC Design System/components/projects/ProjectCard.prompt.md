Project card — metadata is name, location, scope. Nothing else.

```jsx
<ProjectCard image="…" name="Palm Court Residence" location="Newport Beach, CA" scope="Turnkey" ratio="3 / 4" />
```

Hover dims the image slightly and darkens the rule. Never scale, lift, or shadow on hover.
