Project index — composes ProjectCard. Vary each card's `ratio` (3/4, 4/5, 1/1, 4/3) so the grid never reads as tidy thirds.

```jsx
<ProjectIndex eyebrow="Selected work" title="Projects" columns={3} offsets={[0,96,40]} projects={[…]} />
```
